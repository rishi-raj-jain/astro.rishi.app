---
import { month, weekday } from '@/src/lib/operations'

const { date } = Astro.props
---

<span set:text={`${weekday[date.getDay()]}, ${month[date.getMonth()]} ${date.getDate()} ${date.getFullYear()}`} />
